{
  "name": "mizhi-card-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute mizhi-card-db --local --file=./migrations/0001_create_cards.sql",
    "db:migrate:remote": "wrangler d1 execute mizhi-card-db --remote --file=./migrations/0001_create_cards.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "wrangler": "^3.99.0"
  }
}
